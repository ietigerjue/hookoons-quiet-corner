# Styles

Global Tailwind v4 styles currently live in `src/styles.css` because TanStack Start imports it as a URL in `src/routes/__root.tsx`.

Add new style modules here only when a style cannot be expressed cleanly through Tailwind utilities or shared components.
