# Images

Place the default Open Graph image at:

```text
public/images/og-default.png
```

The SEO config points to `/images/og-default.png`. Until that file exists, pages still build and expose complete Open Graph metadata, but social previews should be checked again after adding the image.
